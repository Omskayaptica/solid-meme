🚀 Ultimate Server Setup Script v3.0
Профессиональный скрипт для автоматической настройки production-сервера на Ubuntu/Debian. Объединяет VPN-туннели (Xray, Hysteria2), веб-сайт на Docker и комплексную безопасность в единое решение.

✨ Что нового в v3.0
Выбор протокола Xray: VLESS+TLS или современный VLESS+Reality

Полная идемпотентность: Безопасный многократный запуск без конфликтов

Расширенная безопасность: Fail2ban, мониторинг, системные харденинги

Резервное копирование: Автоматический бэкап конфигов перед изменением

Профессиональный мониторинг: Prometheus Node Exporter для метрик

Улучшенная сеть: Оптимизация ядра, отдельная docker-сеть

Детальная проверка: Валидация портов, конфигов и статусов сервисов

🎯 Основные возможности
🛡️ Безопасность
Системный харденинг: BBR, swap, настройки ядра

Фаервол UFW: Минималистичные правила, защита SSH

Fail2ban: Защита от brute-force атак

SSH Hardening: Отключение парольной аутентификации, настройка таймаутов

Изоляция сервисов: Запуск VPN от непривилегированного пользователя vpnuser

🔐 VPN сервисы
Xray (VLESS): На выбор TLS или Reality протоколы

Hysteria2: Современный UDP-протокол с маскировкой под трафик

Автоматическая генерация: UUID, ключи, пароли

🌐 Веб-инфраструктура
SSL-сертификаты: Автоматическое получение и обновление от Let's Encrypt

Docker-сайт: Развертывание из любого GitHub репозитория

Docker Compose: Поддержка много-контейнерных приложений

Изолированная сеть: Docker network для веб-сервисов

📊 Мониторинг и логирование
Node Exporter: Метрики системы для Prometheus

Детальное логирование: Все этапы в /var/log/server-setup-*.log

Автоматические бэкапы: Конфиги сохраняются перед изменениями

Статус сервисов: Проверка работоспособности после установки

📋 Требования к системе
ОС: Ubuntu 20.04+ или Debian 11+

Права: Root доступ

Память: Минимум 1 ГБ RAM (рекомендуется 2 ГБ)

Диск: Минимум 10 ГБ свободного места

Сеть: Статический IP-адрес, открытые порты 80 и 443

Домен: Настроенная A-запись на IP сервера (для SSL)

🚀 Быстрый старт
1. Подготовка сервера
bash
# Подключение к серверу (с SSH ключом)
ssh root@your-server-ip

# Добавление SSH ключа (ЕСЛИ ЕЩЁ НЕТ)
mkdir -p ~/.ssh
echo "ВАШ_PUBLIC_SSH_KEY" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
2. Запуск установки
bash
# Скачивание скрипта
curl -O https://raw.githubusercontent.com/your-repo/setup-v3.sh
chmod +x setup-v3.sh

# Запуск (рекомендуется в screen/tmux)
./setup-v3.sh
3. Интерактивная настройка
Скрипт запросит следующие данные:

text
1. Домен (example.com) - должен быть направлен на IP сервера
2. Email для Let's Encrypt - для уведомлений о сертификатах
3. GitHub репозиторий сайта (опционально) - для развертывания
4. Порт для Hysteria2 [38271] - или нажмите Enter для значения по умолчанию
5. Протокол для Xray [1/2]:
   1) VLESS + TLS (стандартный)
   2) VLESS + Reality (рекомендуется, обходит блокировки)
⚙️ Архитектура после установки
text
СЕРВЕР
├── 📁 /root/server-setup/
│   ├── configs/          # Конфигурационные файлы
│   ├── website/          # Код веб-сайта (из GitHub)
│   └── backups/          # Автоматические бэкапы
├── 🔐 VPN сервисы
│   ├── Xray:443/tcp      # VLESS (TLS/Reality)
│   └── Hysteria2:38271/udp # Современный UDP протокол
├── 🌐 Веб-сайт
│   └── Docker:80/tcp     -> https://ваш-домен
└── 🛡️ Безопасность
    ├── UFW              # Фаервол
    ├── Fail2ban         # Защита от атак
    └── Node Exporter    # Мониторинг (порт 9100)
🔧 Управление сервисами
bash
# Статус всех сервисов
systemctl status xray hysteria-server fail2ban

# Логи в реальном времени
journalctl -u xray -f
journalctl -u hysteria-server -f
journalctl -u fail2ban -f

# Перезапуск VPN
systemctl restart xray hysteria-server

# Мониторинг ресурсов
htop
docker stats
📊 Мониторинг
Сервер автоматически устанавливает Prometheus Node Exporter:

yaml
# Доступно по адресу:
Метрики: http://ваш-ip:9100/metrics
# Можно интегрировать с:
# - Prometheus + Grafana
# - Uptime Kuma
# - Zabbix
🔄 Обновление сертификатов
Сертификаты обновляются автоматически каждый месяц:

bash
# Ручное обновление
/usr/local/bin/update-certs.sh

# Просмотр запланированных задач
crontab -l
🚨 Важные предупреждения
⚠️ Безопасность SSH
Скрипт отключает вход по паролю! Убедитесь, что:

Ваш SSH ключ добавлен в ~/.ssh/authorized_keys

Вы можете подключиться по SSH без пароля ДО запуска скрипта

⚠️ Конфликт портов
Xray использует порт 443/tcp

Hysteria2 использует 38271/udp (по умолчанию)

Если порты заняты, скрипт предложит выбрать другие

⚠️ Бэкапы
Конфиги автоматически бэкапируются в:

bash
/root/server-setup/backups/ГГГГММДД-ЧЧММСС/
🐛 Поиск и решение проблем
1. Проверка логов установки
bash
tail -f /var/log/server-setup-*.log
2. Проверка открытых портов
bash
ss -tulpn | grep -E '(443|38271|80|22)'
3. Тестирование сервисов
bash
# Проверка Xray
curl -s https://ваш-домен --connect-timeout 5

# Проверка сайта
docker ps | grep website

# Проверка мониторинга
curl http://localhost:9100/metrics | head -5
4. Распространенные ошибки
text
❌ "Домен не резолвится" - проверьте DNS записи
❌ "Порт уже занят" - выберите другой порт
❌ "Не удалось получить сертификат" - проверьте порт 80
📝 Ручные настройки после установки
Изменение пароля Hysteria2
bash
nano /etc/hysteria/config.yaml
systemctl restart hysteria-server
Добавление новых пользователей Xray
bash
# Сгенерировать новый UUID
cat /proc/sys/kernel/random/uuid

# Добавить в конфиг
nano /usr/local/etc/xray/config.json
systemctl restart xray
Настройка Docker сайта
bash
cd /root/server-setup/website
nano docker-compose.yml
docker compose up -d --build
🔄 Миграция с предыдущих версий
Если у вас была v2.x:

bash
# 1. Остановите старые сервисы
systemctl stop xray hysteria-server

# 2. Запустите новый скрипт
./setup-v3.sh

# 3. Скрипт автоматически:
#    - Создаст бэкапы старых конфигов
#    - Обновит сервисы
#    - Сохранит старые настройки где возможно
📊 Производительность
После установки рекомендуем проверить:

bash
# Скорость сети
curl -sL speedtest-cli | python3

# Использование ресурсов
docker stats
htop

# Оптимизация ядра
sysctl net.ipv4.tcp_congestion_control
🤝 Вклад в проект
Мы приветствуем pull requests! Особенно для:

Поддержки новых ОС

Дополнительных VPN протоколов

Улучшений безопасности

Переводов документации

📄 Лицензия
MIT License - смотрите файл LICENSE.

⚠️ Дисклеймер
ВНИМАНИЕ: Этот скрипт предоставляется "как есть". Автор не несет ответственности за:

Потерю доступа к серверам

Нарушение правил хостинг-провайдеров

Любые другие проблемы, вызванные использованием скрипта

Рекомендуется:

Тестировать на тестовом сервере

Иметь резервные копии важных данных

Понимать, что делает каждая команда

📞 Поддержка
Если у вас есть вопросы или проблемы:

Проверьте Issues

Убедитесь, что логи установки не содержат ошибок

Создайте issue с подробным описанием проблемы

Удачной настройки! 🚀

Версия скрипта: 3.0
*Поддержка: Ubuntu 20.04+, Debian 11+*
