# 🚀 Ultimate Server Setup Script

Автоматизированный скрипт для полной настройки защищенного сервера на базе Ubuntu/Debian. Подходит для развертывания VPN (Xray, Hysteria2), веб-сайта через Docker и базового усиления безопасности (Hardening).

## ✨ Основные возможности

-   **Система:** Настройка Swap-файла, включение алгоритма BBR для ускорения сети.
-   **Безопасность:** 
    -   Настройка фаервола **UFW** (закрытие лишних портов, лимит на SSH).
    -   **Hardening SSH**: отключение входа по паролю, запрет root-логина.
    -   Изоляция сервисов: запуск VPN от имени не привилегированного пользователя `vpnuser`.
-   **VPN сервисы:** 
    -   Установка и настройка **Xray** (VLESS/TCP).
    -   Установка и настройка **Hysteria2**.
-   **Web:**
    -   Автоматическое получение SSL-сертификатов **Let's Encrypt**.
    -   Развертывание сайта из любого GitHub репозитория через **Docker Compose**.
-   **Мониторинг:** Полное логирование всех этапов установки в `/var/log/`.

## 🛠 Подготовка

Перед запуском убедитесь, что:
1. У вас чистая ОС **Ubuntu 22.04+** или **Debian 11+**.
2. Ваш домен направлен (A-запись) на IP вашего сервера.
3. У вас есть SSH-ключ для доступа (так как вход по паролю будет отключен).

🚀 Быстрый старт
Скрипт является интерактивным: вам не нужно править код вручную, он сам запросит необходимые данные при запуске.
Склонируйте репозиторий и перейдите в папку:
code

Bash

git clone https://github.com/Omichpolyebok/solid-meme.git
cd solid-meme

Установите права на исполнение и запустите установку:

chmod +x setup.sh

sudo ./setup.sh

Введите данные по запросу:
DOMAIN — ваш домен (уже направленный на IP сервера).
EMAIL — почта для регистрации SSL-сертификата.
GITHUB_REPO_URL — ссылка на ваш репозиторий с сайтом (Docker-проектом).
⚠️ Важная информация
[!IMPORTANT]

Безопасность SSH
Скрипт отключает вход по паролю (PasswordAuthentication no). Перед запуском убедитесь, что вы добавили свой публичный SSH-ключ в файл ~/.ssh/authorized_keys, иначе вы потеряете доступ к серверу после завершения установки.

[!WARNING]

Пароль Hysteria2

Пароль генерируется автоматически в конце установки. Вы можете найти или изменить его позже в конфиге:

/etc/hysteria/config.yaml

📂 Структура проекта
При установке создается следующая иерархия в папке /root/server-setup/:

.
├── setup.sh              # Основной скрипт автоматизации
├── configs/              # Локальные копии конфигов
│   └── hysteria.yaml     # Шаблон настроек Hysteria2
└── website/              # Директория вашего веб-сайта
    └── [файлы из git]    # Автоматически загруженный контент сайта
📄 Лицензия

Этот проект распространяется под лицензией MIT. Вы можете свободно использовать, изменять и распространять его.

Дисклеймер: Используйте на свой страх и риск. Автор не несет ответственности за возможные перебои в работе сервера.
